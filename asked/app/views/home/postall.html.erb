<body >
  <table class = "table">
    <thead>
      <tr>
        <th scope="col"> 글쓴이</th>
        <th scope="col" > 제목</th>
        <th scope="col"> 작성일자</th>
      </tr>
    </thead>
    <% @post.each do |post| %>
    <tbody>
      <tr>
        <td> <%= post.name%> </td>
        <td><a href="/home/postshow/<%=post.id%>"> <%= post.title%></a> </td>
        <td> <%= post.created_at %> </td>
      </tr>
    </tbody>
    <%end%>
  </table>
  <hr />
  <h4><a href="/home/postnew">글 쓰러 가기</a></h4>
</body>
